
멤버변수로 메모리에 안 올려지는 것들은 참조변수를 쓴다
new로 인스턴스해서 참조변수 쓰라는 말

main은 함수이지만 시작점일 뿐. class의 구성원이 아니다.


함수에 
static 키워드가 있을 때 > 클래스이름.함수이름
static 키워드 없을 때 > 참조변수 이용해서 호출하면 됌

Data_8, 9로 알았으면 하는 것
static 키워드 여부로 자원 사용 방법을 구별할 수 있으면 좋겠다.


내 자원을 메모리에 어떻게 올려야 하는가를 공부해야 함
